const counter = {};

setTimeout(() => {
    // increment counter here

    console.log(`Counter after increment: `);
}, 1000);

setTimeout(() => {
    // increment counter here

    console.log(`Counter after increment: `);
}, 2000);

setTimeout(() => {
    // decrement counter here

    console.log(`Counter after decrement: `);
}, 3000);

setTimeout(() => {
    // reset counter here

    console.log(`Counter after reset: `);
}, 4000);